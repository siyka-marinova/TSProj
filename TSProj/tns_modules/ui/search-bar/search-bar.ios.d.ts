import common = require("ui/search-bar/search-bar-common");
export declare class SearchBar extends common.SearchBar {
    private _ios;
    private _delegate;
    constructor();
    public ios : UISearchBar;
}
